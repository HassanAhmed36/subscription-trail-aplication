 <div class="card-body text-center">
     <h2 class="mb-3 fw-bold">${{ $data->amount }}</h2>
     <p class="mb-4">{{ $data->name }}</p>
     <span>we will give 5 days trail aftre we will charge {{ $data->amount }}</span>
 </div>
